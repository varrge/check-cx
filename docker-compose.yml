services:
  check-cx:
    build:
      context: .
    image: check-cx:local
    container_name: check-cx
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
